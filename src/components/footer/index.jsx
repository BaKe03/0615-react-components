
export const Footer = ({ fan }) => {
    return (
        <header>
            <div style={{ color: "white", paddingTop: "1rem" }}>
                Footer
            </div>
        </header>
    );
};